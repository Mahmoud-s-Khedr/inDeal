meta {
  name: Remove Agent
  type: http
  seq: 16
}

delete {
  url: {{base_url}}/companies/me/agents/{{user_id}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{auth_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}