meta {
  name: Get Support Ticket
  type: http
  seq: 4
}

get {
  url: {{base_url}}/support/tickets/{{ticket_id}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{auth_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}