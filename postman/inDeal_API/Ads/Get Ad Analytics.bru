meta {
  name: Get Ad Analytics
  type: http
  seq: 8
}

get {
  url: {{base_url}}/ads/{{ad_id}}/analytics
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{auth_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}