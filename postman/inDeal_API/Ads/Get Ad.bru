meta {
  name: Get Ad
  type: http
  seq: 5
}

get {
  url: {{base_url}}/ads/{{ad_id}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{auth_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}