meta {
  name: Delete Ad
  type: http
  seq: 7
}

delete {
  url: {{base_url}}/ads/{{ad_id}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{auth_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}