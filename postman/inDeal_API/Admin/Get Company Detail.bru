meta {
  name: Get Company Detail
  type: http
  seq: 3
}

get {
  url: {{base_url}}/admin/companies/{{company_id}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{auth_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
