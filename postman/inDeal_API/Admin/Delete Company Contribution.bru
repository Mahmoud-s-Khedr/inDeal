meta {
  name: Delete Company Contribution
  type: http
  seq: 22
}

delete {
  url: {{base_url}}/admin/companies/{{company_id}}/contributions/1
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{auth_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}