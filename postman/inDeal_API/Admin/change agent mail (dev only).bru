meta {
  name: change agent mail (dev only)
  type: http
  seq: 34
}

patch {
  url: {{base_url}}/admin/agents/2/email
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{auth_token}}
}

body:json {
  {
    "email":"mohand.test@gmail.com"
  }
}

vars:pre-request {
  id: 5
}

settings {
  encodeUrl: true
  timeout: 0
}
