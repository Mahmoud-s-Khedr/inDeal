meta {
  name: Delete Company Gallery Item
  type: http
  seq: 14
}

delete {
  url: {{base_url}}/admin/companies/{{company_id}}/gallery/1
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{auth_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}