meta {
  name: List Companies
  type: http
  seq: 1
}

get {
  url: {{base_url}}/admin/companies
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{auth_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
