meta {
  name: Delete Company Document
  type: http
  seq: 18
}

delete {
  url: {{base_url}}/admin/companies/{{company_id}}/documents/1
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{auth_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}