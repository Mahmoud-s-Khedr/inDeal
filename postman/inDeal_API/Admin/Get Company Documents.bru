meta {
  name: Get Company Documents
  type: http
  seq: 15
}

get {
  url: {{base_url}}/admin/companies/{{company_id}}/documents
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{auth_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}