meta {
  name: verify-otp
  type: http
  seq: 7
}

post {
  url: {{base_url}}/auth/verify-otp
  body: json
  auth: inherit
}

body:json {
  {
    "email": "valizylu@denipl.net",
    "otp": "307744" 
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
