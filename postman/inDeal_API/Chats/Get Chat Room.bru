meta {
  name: Get Chat Room
  type: http
  seq: 3
}

get {
  url: {{base_url}}/chats/{{room_id}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{auth_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}