meta {
  name: List Messages
  type: http
  seq: 5
}

get {
  url: {{base_url}}/chats/{{room_id}}/messages
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{auth_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}