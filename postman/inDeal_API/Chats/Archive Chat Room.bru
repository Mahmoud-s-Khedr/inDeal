meta {
  name: Archive Chat Room
  type: http
  seq: 4
}

patch {
  url: {{base_url}}/chats/{{room_id}}/archive
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{auth_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}