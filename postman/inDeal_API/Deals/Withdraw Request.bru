meta {
  name: Withdraw Request
  type: http
  seq: 11
}

delete {
  url: {{base_url}}/deals/requests/{{request_id}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{auth_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}